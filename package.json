{
  "name": "myip-worker",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that returns the client's public IP address",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'Using Cloudflare built-in geolocation data - no build required'",
    "preview": "wrangler preview"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "ip",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.29.0"
  }
}
